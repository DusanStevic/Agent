# DATABASE
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:password}
spring.datasource.url=jdbc:postgresql://${DATABASE_DOMAIN:localhost}:${DATABASE_PORT:5432}/${DATABASE_SCHEMA:servers}
spring.datasource.platform=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

server.port=8080
spring.application.name=report

#app.product.url = ${PRODUCT_SERVICE_PROTOCOL:http}://${PRODUCT_SERVICE_DOMAIN:localhost}:${PRODUCT_SERVICE_PORT:8080}
#app.shop.url = ${SHOP_SERVICE_PROTOCOL:http}://${SHOP_SERVICE_DOMAIN:localhost}:${SHOP_SERVICE_PORT:8080}
app.product.url = https://dule-agent-product-stage.herokuapp.com
app.shop.url = https://dule-agent-shopping-stage.herokuapp.com

jwt.secret=${JWT_SECRET:agent}